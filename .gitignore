# Build artifacts, including generated component-state.env
build/

# Wine prefix + COW clone of the real prefix
prefix/
prefix-copy/
prefix-copy.new.*

# Isolated HOME/XDG tree holding generated yabridge bridges
isolation/
isolation.new.*

# Record of what the last launch actually was, kept outside every writable tree
run-state/

# Generated environment (absolute paths)
env.sh

# Local editor and project metadata
.claude/
.index.yaml
.superpowers/
.worktrees/

# Machine-specific investigation log
AUDIT-LOG.md

# Python
__pycache__/
*.py[cod]
.venv/
